export const ERROR_TEXT = {
    HANDLER: 'Не передан handler',
    CONFIG: 'Не передан config',
}

// Обязательные поля для "DataHelper".
export const REQUIRED_FIELDS = ['handler', 'config'];

export const DEFAULT_PLAYER_NAME = 'player';